query ReviewsRaitingsGetByProductId($productId: ID!, $stage: Stage!) {
	reviews(where: { product: { id: $productId } }, stage: $stage, orderBy: createdAt_DESC) {
		rating
	}
	reviewsConnection(where: { product: { id: $productId } }) {
		aggregate {
			count
		}
	}
}
